{% macro appointmentJourneyTitle(pageName, appointmentJourney) -%}
  {% if appointmentJourney.mode == AppointmentJourneyMode.EDIT %}
    {{ ("Repeating " if appointmentJourney.repeat == YesNo.YES else "One-off ") + pageName }}
  {% else %}
    {{ pageName }}
  {% endif %}
{%- endmacro %}
