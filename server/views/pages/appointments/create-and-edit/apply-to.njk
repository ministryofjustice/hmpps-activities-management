{% extends "layout.njk" %}

{% from "govuk/components/radios/macro.njk" import govukRadios %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "pages/appointments/partials/appointment-type-caption.njk" import appointmentTypeCaption %}
{% from "pages/appointments/partials/appointment-journey-title.njk" import appointmentJourneyTitle %}

{% set pageTitle = appointmentJourneyTitle("Apply to", session.appointmentJourney) %}
{% set pageId = 'appointments-manage-apply-to-page' %}
{% set backLinkHref = "/appointments/" + appointmentId + "/occurrence/" + occurrenceId + "/edit/" + property %}

{% set applyToItems = [] %}
{% for applyToOption in applyToOptions %}
    {% set applyToItems = (applyToItems.push({
        value: applyToOption,
        text: ("Apply the change to this appointment only" if applyToOption == EditApplyTo.THIS_OCCURRENCE) +
              ("Apply the change to this appointment and all the appointments after it in the series" if applyToOption == EditApplyTo.THIS_AND_ALL_FUTURE_OCCURRENCES and session.editAppointmentJourney.sequenceNumber + 1 < session.editAppointmentJourney.repeatCount) +
              ("Apply the change to this and the following appointment in the series" if applyToOption == EditApplyTo.THIS_AND_ALL_FUTURE_OCCURRENCES and session.editAppointmentJourney.sequenceNumber + 1 == session.editAppointmentJourney.repeatCount) +
              ("Apply the change to all appointments in the series" if applyToOption == EditApplyTo.ALL_FUTURE_OCCURRENCES and session.editAppointmentJourney.repeatCount == session.editAppointmentJourney.occurrencesRemaining) +
              ("Apply the change to all remaining appointments in the series" if applyToOption == EditApplyTo.ALL_FUTURE_OCCURRENCES and session.editAppointmentJourney.repeatCount > session.editAppointmentJourney.occurrencesRemaining),
        hint: {
            text: "This option will change " +
                  (session.editAppointmentJourney.repeatCount - session.editAppointmentJourney.sequenceNumber + 1 if applyToOption == EditApplyTo.THIS_AND_ALL_FUTURE_OCCURRENCES) +
                  (session.editAppointmentJourney.occurrencesRemaining if applyToOption == EditApplyTo.ALL_FUTURE_OCCURRENCES) +
                  " appointments"
        } if applyToOption != EditApplyTo.THIS_OCCURRENCE
    } ), categoryOptions) %}
{% endfor %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            {{ appointmentTypeCaption(session.appointmentJourney.type) }}
            <form method="POST">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                {{ govukRadios({
                    name: "applyTo",
                    fieldset: {
                        legend: {
                            text: ("How should this change be applied to the appointments in the series?" if session.editAppointmentJourney.repeatCount == session.editAppointmentJourney.occurrencesRemaining) +
                                  ("How should this change be applied to the remaining appointments in the series?" if session.editAppointmentJourney.repeatCount > session.editAppointmentJourney.occurrencesRemaining),
                            classes: "govuk-label--l",
                            isPageHeading: true
                        }
                    },
                    errorMessage: validationErrors | findError('applyTo'),
                    items: applyToItems
                }) }}

                {{ govukButton({
                    text: "Accept and save"
                }) }}
            </form>
        </div>
    </div>
{% endblock %}
