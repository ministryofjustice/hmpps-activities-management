{% extends "layout.njk" %}

{% from "govuk/components/select/macro.njk" import govukSelect %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "pages/appointments/partials/appointment-journey-title.njk" import appointmentJourneyTitle %}
{% from "pages/appointments/partials/appointment-type-caption.njk" import appointmentTypeCaption %}

{% set pageTitle = appointmentJourneyTitle("Appointment name set" if session.appointmentJourney.type == AppointmentType.BULK else "Appointment name", session.appointmentJourney) %}
{% set pageId = 'appointments-create-category-page' %}
{% set backLinkHref = backLinkHref %}

{% set options = [{ value: "-", text: "" }] %}
{% for category in categories %}
    {% set options = (options.push( { value: category.code, text: category.description } ), options) %}
{% endfor %}

{% block meta %}
    <meta name="autocompleteElements" content="categoryCode"/>
{% endblock %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            {{ appointmentTypeCaption(session) }}
            <form method="POST">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                {{ govukSelect({
                    id: "categoryCode",
                    name: "categoryCode",
                    type: "search",
                    label: {
                        text: ("What are the appointments?" if session.appointmentJourney.type == AppointmentType.BULK else "What’s the appointment?"),
                        classes: "govuk-label--l",
                        isPageHeading: true
                    },
                    hint: {
                        text: "Start typing the appointment name and select from the list."
                    },
                    errorMessage: validationErrors | findError('categoryCode'),
                    items: options,
                    value: formResponses.categoryCode or session.appointmentJourney.category.code
                }) }}

                {{ govukButton({
                    text: "Continue"
                }) }}
            </form>
        </div>
    </div>
{% endblock %}
