{% extends "layout.njk" %}

{% from "govuk/components/select/macro.njk" import govukSelect %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "pages/appointments/partials/appointment-journey-title.njk" import appointmentJourneyTitle %}
{% from "pages/appointments/partials/appointment-type-caption.njk" import appointmentTypeCaption %}

{% set pageTitle = appointmentJourneyTitle("Appointment set location" if session.appointmentJourney.type == AppointmentType.BULK else "Appointment location", session.appointmentJourney) %}
{% set pageId = 'appointments-create-location-page' %}
{% set backLinkHref = backLinkHref %}

{% set options = [{ value: "-", text: "" }] %}
{% for location in locations %}
    {% set options = (options.push( { value: location.id, text: location.description }), options) %}
{% endfor %}

{% block meta %}
    <meta name="autocompleteElements" content="locationId"/>
{% endblock %}

{% block content %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            {{ appointmentTypeCaption(session) }}
            <form method="POST">
                <input type="hidden" name="_csrf" value="{{ csrfToken }}" />
                {{ govukSelect({
                    id: "locationId",
                    name: "locationId",
                    label: {
                        text: ("Where will the appointments take place?" if session.appointmentJourney.type == AppointmentType.BULK else "Where will the appointment take place?"),
                        classes: "govuk-label--l",
                        isPageHeading: true
                    },
                    hint: {
                        text: "Start typing a location and select from the list."
                    },
                    errorMessage: validationErrors | findError('locationId'),
                    items: options,
                    value: formResponses.locationId or session.appointmentJourney.location.id
                }) }}

                {{ govukButton({
                    text: "Confirm and save" if isCtaAcceptAndSave else "Continue"
                }) }}
            </form>
        </div>
    </div>
{% endblock %}
