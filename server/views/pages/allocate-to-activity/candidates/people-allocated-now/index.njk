{% extends "../../../../tabsLayout.njk" %}

{% from "govuk/components/table/macro.njk" import govukTable %}

{% set pageTitle = applicationName + " - Manage activities and schedules" %}
{% set mainClasses = "app-container govuk-body" %}
{% set pageId = 'people-allocated-now-page' %}

{% block tabPanel %}
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            {% set rows = [] %}

            {% for allocation in rowData %}
                {% set rows = (rows.push([
                    {
                        attributes: {
                        id: 'name-' + loop.index
                    },
                        text: allocation.name
                    },
                    {
                        attributes: {
                        id: 'prison-number' + loop.index
                    },
                        text: allocation.prisonNumber
                    },
                    {
                        attributes: {
                        id: 'location-' + loop.index
                    },
                        text: allocation.location
                    },
                    {
                        attributes: {
                        id: 'allocation-' + loop.index
                    },
                        text: 'allocation goes here'
                    }
                ]), rows) %}
            {% endfor %}

            {{ govukTable({
                caption: "People allocated now",
                captionClasses: "govuk-heading-m",
                head: [
                    {
                        text: "Name"
                    },
                    {
                        text: "Prisoner number"
                    },
                    {
                        text: "Location"
                    },
                    {
                        text: "Allocation"
                    }
                ],
                rows: rows
            }) }}
        </div>
    </div>
{% endblock %}

